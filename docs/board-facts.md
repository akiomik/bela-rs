# Bela Gem 実機の採取値

フェーズ 0(ボード到着後の最初のタスク)で採取した事実の記録。
各項目は**実機で確認した値のみ**を書く(推測は書かない)。

> **状態: 未着手(ボード未着)。**

## システム

- [ ] `uname -a` の出力(アーキテクチャ・カーネルバージョン):
- [ ] Xenomai のバージョンと世代(Cobalt/Dovetail の確認方法含む):
- [ ] Debian バージョン:
- [ ] ボード上の Bela ソフトのバージョン / ブランチ / コミット
      (`/root/Bela` の git 情報)。**bindgen のヘッダ取得元をこれにピン留めする**:

## ビルド・リンク情報

C++ サンプルを verbose ビルド(`make VERBOSE=1` 等)して採取する。

- [ ] コンパイル時の `-I` インクルードパス一覧:
- [ ] リンク時の `-l` フラグ一覧:
- [ ] ライブラリ探索パス(`-L`)と `libbela*.so` の実際の場所:
- [ ] Xenomai 関連のリンクフラグ(`libcobalt` 等)と wrapper スクリプトの有無:
- [ ] コアごとの render() 拡張 API のシグネチャ(ヘッダで確認):

## 運用

- [ ] Bela IDE デフォルトプログラムの停止・無効化方法:
- [ ] ssh 接続方法(ホスト名・認証):
- [ ] sysroot として同期が必要なパスの一覧:
